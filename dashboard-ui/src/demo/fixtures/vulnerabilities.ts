import type { VulnerabilityListItem, VulnerabilityDetail } from "../../api/types";
import { pastDate } from "../config";

export function getVulnerabilities(): VulnerabilityListItem[] {
  return [
    {
      id: "vuln-001",
      cve_id: "CVE-2024-21762",
      source: "trivy",
      scanner_type: "container",
      severity: "critical",
      cvss_score: 9.8,
      title: "FortiOS Out-of-bounds Write — Remote Code Execution",
      affected_resource: "fortios-proxy:7.4.2",
      status: "in_progress",
      assigned_team_id: "team-security",
      sla_breached: true,
      first_seen_at: pastDate(14),
      created_at: pastDate(14),
    },
    {
      id: "vuln-002",
      cve_id: "CVE-2024-3094",
      source: "trivy",
      scanner_type: "container",
      severity: "critical",
      cvss_score: 10.0,
      title: "XZ Utils Backdoor — Supply Chain Compromise",
      affected_resource: "base-image:ubuntu-22.04",
      status: "remediated",
      assigned_team_id: "team-platform",
      sla_breached: false,
      first_seen_at: pastDate(21),
      created_at: pastDate(21),
    },
    {
      id: "vuln-003",
      cve_id: "CVE-2023-44487",
      source: "grype",
      scanner_type: "container",
      severity: "critical",
      cvss_score: 7.5,
      title: "HTTP/2 Rapid Reset — Denial of Service",
      affected_resource: "nginx-ingress:1.9.4",
      status: "triaged",
      assigned_team_id: "team-platform",
      sla_breached: true,
      first_seen_at: pastDate(7),
      created_at: pastDate(7),
    },
    {
      id: "vuln-004",
      cve_id: "CVE-2024-0567",
      source: "snyk",
      scanner_type: "dependency",
      severity: "high",
      cvss_score: 7.5,
      title: "GnuTLS Certificate Verification Bypass",
      affected_resource: "auth-service:latest",
      status: "in_progress",
      assigned_team_id: "team-backend",
      sla_breached: false,
      first_seen_at: pastDate(5),
      created_at: pastDate(5),
    },
    {
      id: "vuln-005",
      cve_id: "CVE-2024-1086",
      source: "trivy",
      scanner_type: "host",
      severity: "high",
      cvss_score: 7.8,
      title: "Linux Kernel nf_tables Use-After-Free — Local Privilege Escalation",
      affected_resource: "gke-node-pool-1",
      status: "new",
      assigned_team_id: null,
      sla_breached: false,
      first_seen_at: pastDate(2),
      created_at: pastDate(2),
    },
    {
      id: "vuln-006",
      cve_id: null,
      source: "semgrep",
      scanner_type: "sast",
      severity: "medium",
      cvss_score: 5.4,
      title: "SQL Injection in analytics query builder",
      affected_resource: "analytics-service/src/queries.py",
      status: "in_progress",
      assigned_team_id: "team-backend",
      sla_breached: false,
      first_seen_at: pastDate(10),
      created_at: pastDate(10),
    },
  ];
}

export function getVulnerabilityDetail(id: string): VulnerabilityDetail | null {
  const items = getVulnerabilities();
  const base = items.find((v) => v.id === id);
  if (!base) return null;

  return {
    id: base.id,
    cve_id: base.cve_id,
    scan_id: "scan-001",
    source: base.source,
    scanner_type: base.scanner_type,
    severity: base.severity,
    cvss_score: base.cvss_score,
    title: base.title,
    description: `${base.title}. This vulnerability affects ${base.affected_resource} and has a CVSS score of ${base.cvss_score}. Immediate patching is recommended.`,
    package_name: base.affected_resource.split(":")[0],
    affected_resource: base.affected_resource,
    status: base.status,
    assigned_team_id: base.assigned_team_id,
    assigned_user_id: null,
    sla_due_at: pastDate(-3),
    sla_breached: base.sla_breached,
    first_seen_at: base.first_seen_at,
    last_seen_at: pastDate(0),
    remediated_at: base.status === "remediated" ? pastDate(1) : null,
    closed_at: null,
    remediation_steps: [
      { step: "Update affected package to latest patched version" },
      { step: "Rebuild and redeploy affected containers" },
      { step: "Verify fix with targeted vulnerability scan" },
    ],
    scan_metadata: {},
    comments: [
      {
        id: "comment-001",
        vulnerability_id: base.id,
        user_id: "user-001",
        content: "Prioritizing this for next sprint. Patch available upstream.",
        comment_type: "note",
        metadata: {},
        created_at: pastDate(1),
      },
    ],
    created_at: base.created_at,
    updated_at: pastDate(0),
  };
}
